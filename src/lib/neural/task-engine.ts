export const executeBusinessTask = async (taskType: string, businessId: string) => {
  // 1. Fetch historical data from the business_roi_stats table
  // 2. Pass data to AI with the prompt: "Act as a CFO for this business..."
  // 3. Return a formatted table or draft.
};
