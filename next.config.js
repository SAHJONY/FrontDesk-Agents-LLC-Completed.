/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any specific Next.js configuration settings here if needed.
  // Example:
  // reactStrictMode: true,
};

module.exports = nextConfig;
