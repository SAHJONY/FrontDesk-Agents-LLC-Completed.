'use client';

import React, { useMemo, useState, Suspense } from 'react';
import Link from 'next/link';
import { Check, Shield, TrendingUp, Loader2 } from 'lucide-react';

/**
 * PRICING PAGE - SOVEREIGN INFRASTRUCTURE V2.2.1
 * Includes:
 * 1. Interactive Billing Cycle Toggle (Monthly/Annual 20% Discount)
 * 2. Fail-safe Backgrounds (handles missing 8K assets gracefully)
 * 3. Enterprise-grade Feature Matrix
 */

const ANNUAL_DISCOUNT = 0.2;
type BillingCycle = 'monthly' | 'annual';

function formatMoney(n: number) {
  return n.toLocaleString('en-US');
}

function computePricing(priceMonthly: number, billingCycle: BillingCycle) {
  if (billingCycle === 'monthly') {
    return { displayMonthly: priceMonthly, billedText: null };
  }
  const discountedMonthly = Math.round(priceMonthly * (1 - ANNUAL_DISCOUNT));
  const annualTotal = Math.round(priceMonthly * 12 * (1 - ANNUAL_DISCOUNT));
  return {
    displayMonthly: discountedMonthly,
    billedText: `Billed $${formatMoney(annualTotal)}/yr (save 20%)`,
  };
}

const pricingTiers = [
  {
    name: 'Starter',
    tagline: 'Ideal for solo operators',
    price: 199,
    description: 'Autonomous AI receptionist with dedicated node access',
    features: {
      'Sovereign Infrastructure': [
        { name: '300 Included Minutes', included: true },
        { name: 'Global Node activation', included: true },
        { name: 'Overage protection', value: '$0.45/min' },
      ],
      'AI Fleet Ops': [
        { name: '24/7 autonomous intake', included: true },
        { name: 'Natural-language scripts', included: true },
      ],
      'Limits': [
        { name: 'Users', value: '1' },
        { name: 'Uptime SLA', value: '99.5%' },
      ],
    },
    cta: 'Activate Node',
    popular: false,
    accent: 'emerald'
  },
  {
    name: 'Professional',
    tagline: 'Built for growth',
    price: 399,
    description: 'High-capacity AI workforce with priority routing',
    features: {
      'Sovereign Infrastructure': [
        { name: '1,200 Included Minutes', included: true },
        { name: 'Priority Node status', included: true },
        { name: 'Reduced overages', value: '$0.40/min' },
      ],
      'AI Fleet Ops': [
        { name: 'Advanced Script Engine', included: true },
        { name: '50+ Languages (Native)', included: true },
      ],
      'Limits': [
        { name: 'Users', value: 'Up to 5' },
        { name: 'Uptime SLA', value: '99.9%' },
      ],
    },
    cta: 'Scale Fleet',
    popular: true,
    accent: 'cyan'
  },
  {
    name: 'Growth',
    tagline: 'Multi-location dominance',
    price: 799,
    description: 'Large-scale infrastructure for high-volume revenue recovery',
    features: {
      'Sovereign Infrastructure': [
        { name: '3,000 Included Minutes', included: true },
        { name: 'Multi-region Node cluster', included: true },
        { name: 'Volume overages', value: '$0.35/min' },
      ],
      'AI Fleet Ops': [
        { name: 'Full CRM Integration', included: true },
        { name: 'Custom Voice Cloning', included: true },
      ],
      'Limits': [
        { name: 'Users', value: 'Up to 15' },
        { name: 'Uptime SLA', value: '99.95%' },
      ],
    },
    cta: 'Establish Cluster',
    popular: false,
    accent: 'cyan'
  },
  {
    name: 'Enterprise',
    tagline: 'The Revenue Standard',
    price: 1499,
    description: 'Infinite scale with performance-based royalties',
    features: {
      'Sovereign Infrastructure': [
        { name: '7,000 Included Minutes', included: true },
        { name: 'Dedicated Bare-metal Node', included: true },
        { name: 'Elite overages', value: '$0.30/min' },
      ],
      'Security & Success': [
        { name: 'TCPA/DNC Enforcement', included: true },
        { name: 'Dedicated Success Mgr', included: true },
      ],
      'Limits': [
        { name: 'Users', value: 'Unlimited' },
        { name: 'Uptime SLA', value: '99.99%' },
      ],
    },
    cta: 'Consult Sovereignty',
    popular: false,
    accent: 'cyan'
  },
];

function PricingContent() {
  const [billingCycle, setBillingCycle] = useState<BillingCycle>('monthly');

  const computed = useMemo(() => {
    return pricingTiers.map((tier) => ({
      ...tier,
      pricing: computePricing(tier.price, billingCycle),
    }));
  }, [billingCycle]);

  return (
    <div className="min-h-screen bg-[#0a0a0a] text-white selection:bg-cyan-500/30">
      
      {/* 🛡️ FAIL-SAFE HEADER (Gradients replace missing 8K Hero) */}
      <div className="relative py-24 px-4 overflow-hidden">
        <div className="absolute top-0 left-1/2 -translate-x-1/2 w-full h-full bg-[radial-gradient(circle_at_center,_rgba(6,182,212,0.12),transparent_70%)] -z-10" />
        <div className="max-w-7xl mx-auto text-center">
          <div className="inline-flex items-center gap-2 rounded-full border border-cyan-500/20 bg-cyan-500/5 px-4 py-1.5 text-[10px] font-black uppercase tracking-[0.3em] text-cyan-500 mb-8">
            Neural Infrastructure v2.2.1
          </div>
          <h1 className="text-5xl md:text-8xl font-black mb-6 italic tracking-tighter uppercase leading-none">
            Global <span className="text-cyan-500 text-stroke-sm">Infrastructure</span>
          </h1>
          <p className="text-lg text-gray-400 mb-12 max-w-2xl mx-auto font-medium opacity-80">
            Select your specialized AI node tier to activate and scale your autonomous fleet.
          </p>

          <div className="inline-flex items-center gap-4 bg-zinc-900/50 border border-white/5 rounded-full p-1.5 backdrop-blur-md">
            <button
              onClick={() => setBillingCycle('monthly')}
              className={`px-8 py-2.5 rounded-full font-black text-[10px] uppercase tracking-widest transition-all ${
                billingCycle === 'monthly' ? 'bg-white text-black' : 'text-gray-500 hover:text-white'
              }`}
            >
              Monthly
            </button>
            <button
              onClick={() => setBillingCycle('annual')}
              className={`px-8 py-2.5 rounded-full font-black text-[10px] uppercase tracking-widest transition-all flex items-center gap-2 ${
                billingCycle === 'annual' ? 'bg-white text-black' : 'text-gray-500 hover:text-white'
              }`}
            >
              Annual
              <span className="text-[9px] bg-emerald-500 text-white px-2 py-0.5 rounded-full">
                -20%
              </span>
            </button>
          </div>
        </div>
      </div>

      {/* PRICING GRID */}
      <div className="max-w-[1500px] mx-auto px-6 mb-32">
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
          {computed.map((tier, index) => (
            <div
              key={index}
              className={`relative bg-white/[0.02] rounded-[2.5rem] border transition-all duration-500 ${
                tier.popular ? 'border-cyan-500/50 bg-cyan-500/[0.03] scale-[1.02] shadow-[0_0_40px_rgba(6,182,212,0.1)]' : 'border-white/10'
              } p-8 flex flex-col hover:border-white/20`}
            >
              <div className="mb-8">
                <h3 className={`text-[10px] font-black uppercase tracking-[0.4em] mb-4 ${
                  tier.accent === 'emerald' ? 'text-emerald-500' : 'text-cyan-500'
                }`}>
                  {tier.name} Division
                </h3>

                <div className="flex items-baseline gap-1 mb-2">
                  <span className="text-5xl font-black italic tracking-tighter">${formatMoney(tier.pricing.displayMonthly)}</span>
                  <span className="text-zinc-600 font-bold text-xs uppercase ml-1">/mo</span>
                </div>

                {tier.pricing.billedText && (
                  <p className="text-[10px] text-emerald-400 font-black uppercase tracking-wider mb-2">
                    {tier.pricing.billedText}
                  </p>
                )}

                <p className="text-zinc-500 text-xs font-bold uppercase tracking-tight mt-4 min-h-[40px] leading-relaxed">
                  {tier.description}
                </p>
              </div>

              <Link
                href={`/signup?plan=${tier.name.toLowerCase()}&billing=${billingCycle}`}
                className={`w-full py-4 rounded-2xl font-black uppercase tracking-[0.2em] text-[10px] text-center transition-all mb-10 ${
                  tier.popular
                    ? 'bg-cyan-500 text-black hover:bg-cyan-400'
                    : 'bg-white text-black hover:bg-zinc-200'
                }`}
              >
                {tier.cta}
              </Link>

              <div className="space-y-8 flex-1">
                {Object.entries(tier.features).map(([category, features]) => (
                  <div key={category}>
                    <h4 className="text-[9px] font-black text-zinc-600 uppercase tracking-widest mb-4 border-b border-white/5 pb-2">
                      {category}
                    </h4>
                    <ul className="space-y-4">
                      {features.map((feature: any, idx: number) => (
                        <li key={idx} className="flex items-start gap-3">
                          <Check className={`w-4 h-4 flex-shrink-0 mt-0.5 ${
                            tier.accent === 'emerald' ? 'text-emerald-500' : 'text-cyan-500'
                          }`} />
                          <div className="flex flex-col">
                            <span className="text-xs font-bold text-zinc-300 uppercase tracking-tight">{feature.name}</span>
                            {feature.value && (
                              <span className="text-white font-black uppercase text-[9px] mt-1 tracking-widest opacity-60">
                                {feature.value}
                              </span>
                            )}
                          </div>
                        </li>
                      ))}
                    </ul>
                  </div>
                ))}
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* TRUST & ROI SECTION */}
      <div className="max-w-5xl mx-auto px-6 pb-40 border-t border-white/5 pt-20">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-16">
          <div className="flex gap-6">
            <div className="p-4 rounded-2xl bg-cyan-500/10 border border-cyan-500/20 h-fit">
              <Shield className="w-8 h-8 text-cyan-500" />
            </div>
            <div>
              <h4 className="font-black italic uppercase tracking-tighter text-xl mb-3">Privacy Enforcement</h4>
              <p className="text-sm text-zinc-500 leading-relaxed font-medium">
                Sovereign data principles. We do not use your proprietary interactions to train public models. Your revenue intelligence remains yours.
              </p>
            </div>
          </div>
          <div className="flex gap-6">
            <div className="p-4 rounded-2xl bg-emerald-500/10 border border-emerald-500/20 h-fit">
              <TrendingUp className="w-8 h-8 text-emerald-500" />
            </div>
            <div>
              <h4 className="font-black italic uppercase tracking-tighter text-xl mb-3">Revenue Recovery</h4>
              <p className="text-sm text-zinc-500 leading-relaxed font-medium">
                Our infrastructure is designed as a profit center. Enterprise tiers include performance royalty models that align our success with yours.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

export default function PricingPage() {
  return (
    <Suspense fallback={
      <div className="min-h-screen bg-[#0a0a0a] flex flex-col items-center justify-center">
        <Loader2 className="w-10 h-10 text-cyan-500 animate-spin mb-4" />
        <span className="text-[10px] font-black uppercase tracking-[0.4em] text-cyan-500">Initializing Nodes</span>
      </div>
    }>
      <PricingContent />
    </Suspense>
  );
}
