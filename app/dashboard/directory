'use client';

import React from 'react';
import Link from 'next/link';
import { usePathname } from 'next/navigation';

/**
 * FRONTDESK AGENTS: GLOBAL REVENUE WORKFORCE
 * Premium Dashboard Shell with Glassmorphism Sidebar
 */

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();

  const navItems = [
    { name: 'Fleet Overview', href: '/dashboard' },
    { name: 'Revenue Metrics', href: '/dashboard/metrics' },
    { name: 'Agent Settings', href: '/dashboard/settings' },
  ];

  return (
    <div className="flex min-h-screen bg-black text-zinc-100 bg-grid-pattern">
      {/* Sidebar: Premium Glass Panel */}
      <aside className="w-64 border-r border-zinc-800 bg-zinc-950/50 backdrop-blur-xl p-6 hidden md:flex flex-col">
        <div className="mb-10">
          <h2 className="text-xl font-black tracking-tighter text-white">
            FRONTDESK
          </h2>
          <p className="text-[10px] text-blue-500 font-mono tracking-widest uppercase">
            Revenue Workforce
          </p>
        </div>

        <nav className="flex-1 space-y-2">
          {navItems.map((item) => {
            const isActive = pathname === item.href;
            return (
              <Link
                key={item.href}
                href={item.href}
                className={`block px-4 py-2 rounded-lg text-sm font-medium transition-all ${
                  isActive 
                    ? 'bg-blue-600/10 text-blue-400 border border-blue-500/20' 
                    : 'text-zinc-500 hover:text-white hover:bg-zinc-900'
                }`}
              >
                {item.name}
              </Link>
            );
          })}
        </nav>

        <div className="pt-6 border-t border-zinc-900">
          <div className="p-3 rounded-xl bg-zinc-900/50 border border-zinc-800">
            <p className="text-[10px] text-zinc-500 uppercase font-bold">Active Tier</p>
            <p className="text-sm font-bold text-white uppercase tracking-tighter">Elite Workforce</p>
            <p className="text-[9px] text-blue-500 font-mono mt-1">$1,499 / mo Permanent</p>
          </div>
        </div>
      </aside>

      {/* Main Command Center Content */}
      <main className="flex-1 overflow-y-auto p-8">
        <header className="flex justify-between items-center mb-8">
          <div>
            <h1 className="text-3xl font-black tracking-tight">Command Center</h1>
            <p className="text-zinc-500 text-sm">pdx1 Primary Operational Zone Active</p>
          </div>
          <div className="flex items-center gap-4">
             <div className="px-3 py-1 rounded-full bg-green-500/10 border border-green-500/20 text-green-500 text-[10px] font-bold uppercase tracking-widest animate-pulse">
               Systems Nominal
             </div>
          </div>
        </header>
        {children}
      </main>
    </div>
  );
}
