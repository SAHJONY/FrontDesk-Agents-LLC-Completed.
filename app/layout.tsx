'use client';

import "./globals.css";
import { AuthProvider } from "./contexts/AuthContext"; // Point to the internal app path

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
