/**
 * FRONTDESK AGENTS: GLOBAL REVENUE WORKFORCE
 * Core client for authenticated browser-side interactions.
 * Optimized for the pdx1 build region.
 */

import { createClientComponentClient } from '@supabase/auth-helpers-nextjs';

// Note: If you have generated Supabase types, import them here.
// If not, the client will default to <any> which allows the build to pass.
export const supabase = createClientComponentClient();

/**
 * CLIENT CAPABILITIES:
 * - Real-time Node Fleet Monitoring
 * - Regional Multiplier Data Retrieval ($199 - $1,499 tiers)
 * - Managed session persistence for FrontDesk Agents dashboards
 */
