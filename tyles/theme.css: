:root {
  --color-bg-dark: #050915;
  --color-panel: #0e1627;
  --color-accent: #00d1ff;
  --color-accent-soft: #007d99;
  --color-text: #f5f9ff;
}

body {
  background: var(--color-bg-dark);
  color: var(--color-text);
  font-family: "Inter", sans-serif;
}

.card {
  background: var(--color-panel);
  border-radius: 1rem;
  padding: 2rem;
  box-shadow: 0 0 40px rgba(0, 209, 255, 0.08);
}
