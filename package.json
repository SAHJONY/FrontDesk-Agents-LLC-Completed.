// ... other entries in "devDependencies"
"devDependencies": {
    // ... all dev dependencies listed
    "typescript": "^5" // <-- THERE MUST NOT BE A COMMA HERE (since it's the last item in devDependencies)
}, // <-- THERE MUST BE A COMMA HERE (to separate devDependencies from the next object, engines)

"engines": {
  "node": "22.x"
}
// ...
